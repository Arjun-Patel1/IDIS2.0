version: "3.9"

services:
  idis:
    build: .
    container_name: idis_app
    ports:
      - "8000:8000"  # FastAPI
      - "8501:8501"  # Streamlit
    volumes:
      - ./models:/app/models
      - ./feedback:/app/feedback
    environment:
      - PYTHONUNBUFFERED=1
